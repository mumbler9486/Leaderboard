export const serverHandlers = [
	// Here, you can mock absolute URL requests,
	// e.g. to a database.
	// This is also the place to mock absolute
	// SSR-imports. Everything in 'handlers.workers.ts'
	// is mocked client-side.
];
